<%= render partial: "shared/nav" %>

<%= render partial: 'form', locals: { model: nil,
                                      method: :post,
                                      url: merchant_bulk_discounts_path(@merchant),
                                      percent_default: nil,
                                      quantity_default: nil
                                     } %>