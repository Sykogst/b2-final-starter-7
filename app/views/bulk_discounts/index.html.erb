<%= render partial: "shared/nav" %>

<% @bulk_discounts.each do |discount| %>
  <div id=<%= "discount-#{discount.id}" %>>
    <%= "#{discount.percentage}% off #{discount.quantity_threshold} items" %>
  </div>
<% end %>