<%= render partial: "shared/nav" %>

<%= render partial: 'form', locals: { model: @bulk_discount,
                                      method: :patch,
                                      url: merchant_bulk_discount_path(@merchant, @bulk_discount),
                                      percent_default: @bulk_discount.percentage,
                                      quantity_default: @bulk_discount.quantity_threshold
                                     } %>